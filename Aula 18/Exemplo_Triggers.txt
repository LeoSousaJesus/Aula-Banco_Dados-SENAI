CREATE DATABASE TESTE;
USE TESTE;

CREATE TABLE USUARIO(
IDUSUARIO INT PRIMARY KEY auto_increment,
NOME VARCHAR(50) NOT NULL,
LOGIN VARCHAR(50) NOT NULL,
SENHA VARCHAR(50) NOT NULL
);

SHOW TABLES;

CREATE TABLE BKPUSUARIO(
IDBKP INT PRIMARY KEY AUTO_INCREMENT,
IDUSUARIO INT,
NOME VARCHAR (50),
LOGIN VARCHAR(50)
);

DESC USUARIO;
DESC BKPUSUARIO;

INSERT INTO USUARIO VALUES (NULL,'JORGE ANDRE', 'JORGE-ANDRE', 'JORGE123'),
							(NULL, 'ANDRE LUIZ', 'ANDRE-LUIZ', 'ANDRE123'),
                            (NULL, 'KELLY ANDREIA', 'KELLY-ANDREIA','KELLY123');

SELECT * FROM USUARIO;
SELECT * FROM BKPUSUARIO;

DELIMITER $
CREATE TRIGGER SENAI
BEFORE DELETE ON USUARIO
FOR EACH ROW
BEGIN
INSERT INTO BKPUSUARIO VALUES(NULL,OLD.IDUSUARIO, OLD.NOME, OLD.LOGIN);
END
$
DELIMITER ;
DELETE FROM USUARIO
WHERE IDUSUARIO = 3;